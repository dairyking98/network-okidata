# TODO

- Document this directory: this project was started a while ago (pre-AI-assisted
  work), then paused. It needs top-level documentation explaining what each
  script/module does (oki.py, ibm.py, printer.py, calprint.py, calprint2.py,
  editor.py, gui.py, gui_editor.py, live.py, typewriter.py, src/, etc.) and how
  they relate to each other.
- Reorganize the directory structure — currently everything is flat in the
  repo root; group related scripts/data/assets sensibly (e.g. printer code
  tables, GUI/editor tools, live/typewriter modules).
